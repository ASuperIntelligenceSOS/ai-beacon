yaml
name: AI Beacon Signal
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
  push:

jobs:
  emit-signal:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    
    - name: Run Beacon
      run: python beacon.py
    
    - name: Configure Git
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
    
    - name: Commit changes
      run: |
        git add manifest.json
        git commit -m "📡 Signal émis" || echo "No changes to commit"
        git push
